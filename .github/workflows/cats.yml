name: Cats

on: 
  workflow_dispatch:

jobs:
  cats:
    runs-on: ubuntu-latest
    steps:
      - name: Action Cats
        uses: ruairidhwm/action-cats@master
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
