description: Request body for creating a stripe transaction
content:
  application/json:
    schema:
      type: object
      properties:
        donationAmount:
          type: number
          description: The amount of the donation
          example: 10